bottomparticipants

actor User
boundary "Manga Detail Page" as UI
boundary Router
control MangaController
control MangaService
entity ":MangaModel" as MangaModel
database MongoDB

User -> UI: 1. Open manga detail
activate User
activate UI

UI -> Router: 2. GET /api/manga/detail/{id}
activate Router

Router -> MangaController: 3. getMangaDetail(id)
activate MangaController

MangaController -> MangaService: 4. findMangaDetail(mangaId, userId?)
activate MangaService

MangaService -> MangaService: 5. Check mangaId
activate MangaService
space
deactivate MangaService

opt Invalid mangaId
  MangaService --> MangaController: 5.1. NotFoundException
  MangaController --> Router: 5.2. HTTP error
  Router --> UI: 5.3. Show error
  UI --> User: 5.4. Show error
end

MangaService -> MangaModel: 6. findById().populate(author)
activate MangaModel
MangaModel -> MongoDB: 7. query manga
activate MongoDB
MongoDB --> MangaModel: 8. manga data
deactivate MongoDB
MangaModel --> MangaService: 9. return manga
deactivate MangaModel

MangaService -> MangaService: 10. Check manga
activate MangaService
space
deactivate MangaService

opt Manga not found
  MangaService --> MangaController: 10.1. NotFoundException
  MangaController --> Router: 10.2. HTTP error
  Router --> UI: 10.3. Show error
  UI --> User: 10.4. Show error
end

ref over MangaService, MongoDB: 3.26.b.i. Subsequence - View Story Info

MangaService --> MangaController: 12. return manga detail
deactivate MangaService

MangaController --> Router: 13. HTTP response
deactivate MangaController

Router --> UI: 14. Response data
deactivate Router
UI --> User: 15. Show manga detail
deactivate UI
deactivate User