bottomparticipants

actor "User/Author" as A
boundary "Check-in Modal" as UI
boundary Router
control "CheckinController" as Controller
entity "CheckinService" as Service
entity ":CheckinModel" as CheckinModel
entity ":UserModel" as UserModel
database MongoDB

A -> UI : 1. Click "Check-in" button
activate A
activate UI

UI -> Router : 2. POST /api/checkin/today
activate Router

Router -> Controller : 3. checkinToday(req)
activate Controller

Controller -> Service : 4. checkinToday(userId, role)
activate Service

Service -> CheckinModel : 5. findOneAndUpdate(upsert record)
activate CheckinModel
CheckinModel -> MongoDB : 6. Execute query
activate MongoDB
MongoDB --> CheckinModel : 7. document
deactivate MongoDB
CheckinModel --> Service : 8. record
deactivate CheckinModel

Service -> Service: 9. Check already checked today
activate Service
space
deactivate Service

alt already checked
    Service --> Controller : 9.1 BadRequestException
    Controller --> Router : 9.2 HTTP response (400 error)
    Router --> UI : 9.3 error message
    UI --> A : 9.4 Display "Already checked in today!" message
end
    Service -> UserModel : 10. Find user
    activate UserModel
    UserModel -> MongoDB : 11. Execute query
    activate MongoDB
    MongoDB --> UserModel : 12. document
    deactivate MongoDB
    UserModel --> Service : 13. user
    deactivate UserModel

    Service -> CheckinModel : 14. Save record
    activate CheckinModel
    CheckinModel --> Service : 15. success
    deactivate CheckinModel

    Service -> UserModel : 16. updated points
    activate UserModel
    UserModel --> Service: 17. success
    deactivate UserModel

    Service --> Controller : 18. success
    deactivate Service
    Controller --> Router : 19. HTTP response (200 OK)
    deactivate Controller
    Router --> UI : 20. success message
    deactivate Router
    UI --> A : 21. Disable "Check-in" button & Mark and claim reward
    deactivate UI
    deactivate A